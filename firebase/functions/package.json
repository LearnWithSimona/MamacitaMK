{
  "name": "mamacitamk-scraper",
  "description": "Standalone Node script that scrapes baby-store sites and writes products to Firestore. Runs from GitHub Actions on a cron schedule.",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "node lib/index.js"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
